<h1>Listing actcodes</h1>

<table>
  <tr>
    <th>Actcode</th>
    <th>Management Company</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @actcodes.each do |actcode| %>
  <tr>
    <td><%= actcode.actcode %></td>
    <td><%= actcode.management.name %></td>
    <td><%= link_to 'Show', actcode %></td>
    <td><%= link_to 'Edit', edit_actcode_path(actcode) %></td>
    <td><%= link_to 'Destroy', actcode, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Actcode', new_actcode_path %>
