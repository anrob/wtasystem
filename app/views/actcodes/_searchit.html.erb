Search By:
<%= form_for @search, url: actcodes_path, html: {method: :get} do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description_contains %><br />
  <%#= f.label :comments_created_at_greater_than, 'With comments after' %>

  <!-- etc... -->
  <%= f.submit %>
<% end %>
